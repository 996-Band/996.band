{
  "name": "996-band",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nuxt": "^2.15.8",
    "smoothscroll-polyfill": "^0.4.3"
  }
}
